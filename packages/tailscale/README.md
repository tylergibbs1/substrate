# @substrate/tailscale

Remote-connection helpers, reused pattern from t3code (PRD §9, §6.5). Stub.

Purpose: expose the local MCP server to trusted remote clients over a Tailscale
tailnet instead of the public internet. Off by default; token-gated when on.
